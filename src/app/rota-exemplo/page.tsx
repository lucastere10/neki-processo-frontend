import Title from '@/components/Title';
import { FC } from 'react';

const RotaExemplo: FC<unknown> = () => {
  return <Title title="Esta é uma rota de Exemplo: /rota-exemplo" />;
};

export default RotaExemplo;
